name: Build OSXCross Release for macOS High Sierra

on:
  push:
    branches:
      - main
      - develop
  workflow_dispatch:
  schedule:
    - cron: '0 0 1 * *'

env:
  OSXCROSS_SDK_VERSION: '10.13'
  BUILD_PREFIX: '/opt/osxcross'
  JOBS: 16

jobs:
  build-osxcross-complete:
    runs-on: ubuntu-latest
    timeout-minutes: 600
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install base dependencies
        run: |
          sudo apt-get update
          sudo apt-get dist-upgrade -y
          sudo apt-get install -y build-essential cmake git wget curl gpg autoconf automake libtool libtool-bin pkg-config texinfo bison flex help2man gawk sed gzip bzip2 xz-utils zlib1g-dev libssl-dev libncurses5-dev libreadline-dev libgdbm-dev libsqlite3-dev libffi-dev uuid-dev python3-dev python3-pip clang llvm lld lldb mingw-w64 dosfstools hfsplus xorriso libpng-dev libjpeg-turbo8-dev libtiff-dev libwebp-dev libopenjp2-7-dev libfreetype6-dev libfontconfig1-dev libharfbuzz-dev libgomp1 libmpfr-dev libmpc-dev libisl-dev libelf-dev libzstd-dev libdebuginfod-dev ninja-build

      - name: Create build directory structure
        run: |
          sudo mkdir -p ${BUILD_PREFIX}/{target,SDK,libs,tarballs,sources}
          sudo chown -R $(id -u):$(id -g) ${BUILD_PREFIX}

      - name: Clone osxcross repository
        run: |
          cd ${BUILD_PREFIX}
          git clone --recursive https://github.com/tpoechtrager/osxcross.git src

      - name: Download macOS High Sierra SDK
        run: |
          cd ${BUILD_PREFIX}/src/tarballs
          echo "Downloading SDK to osxcross tarballs directory..."
          wget -q -T 60 --tries=5 -O MacOSX10.13.sdk.tar.xz https://github.com/joseluisq/macosx-sdks/releases/download/10.13/MacOSX10.13.sdk.tar.xz
          if [ ! -f MacOSX10.13.sdk.tar.xz ]; then
            echo "First mirror failed, trying alternative..."
            wget -q -T 60 --tries=5 -O MacOSX10.13.sdk.tar.xz https://github.com/phracker/MacOSX-SDKs/releases/download/10.13/MacOSX10.13.sdk.tar.xz
          fi
          ls -lh

      - name: Build osxcross toolchain
        run: |
          cd ${BUILD_PREFIX}/src
          export UNATTENDED=1
          export FORCE_PACK_EDH=1
          export OSXCROSS_ROOT=${BUILD_PREFIX}/src
          export SDK_VERSION=10.13
          export JOBS=16
          ./build.sh

      - name: Copy osxcross to final location
        run: |
          cp -r ${BUILD_PREFIX}/src/target/* ${BUILD_PREFIX}/target/
          if [ -d "${BUILD_PREFIX}/src/SDK" ]; then
            cp -r ${BUILD_PREFIX}/src/SDK/* ${BUILD_PREFIX}/SDK/
          fi

      - name: Verify toolchain
        run: |
          export PATH=${BUILD_PREFIX}/target/bin:$PATH
          which x86_64-apple-darwin17-clang
          x86_64-apple-darwin17-clang --version

      - name: Build OpenSSL
        continue-on-error: true
        run: |
          export PATH=${BUILD_PREFIX}/target/bin:$PATH
          export OSXCROSS_ROOT=${BUILD_PREFIX}
          export CC=x86_64-apple-darwin17-clang
          export CXX=x86_64-apple-darwin17-clang++
          export CFLAGS="-mmacosx-version-min=10.13"
          export CXXFLAGS="-mmacosx-version-min=10.13"
          export LD_LIBRARY_PATH=${BUILD_PREFIX}/libs/lib:$LD_LIBRARY_PATH
          cd ${BUILD_PREFIX}/sources
          wget -q https://www.openssl.org/source/openssl-1.1.1w.tar.gz
          tar xzf openssl-1.1.1w.tar.gz
          cd openssl-1.1.1w
          ./config shared --prefix=${BUILD_PREFIX}/libs/openssl
          make -j16
          make install

      - name: Build zlib
        continue-on-error: true
        run: |
          export PATH=${BUILD_PREFIX}/target/bin:$PATH
          export CC=x86_64-apple-darwin17-clang
          export CFLAGS="-mmacosx-version-min=10.13"
          cd ${BUILD_PREFIX}/sources
          wget -q https://github.com/madler/zlib/archive/refs/tags/v1.3.1.tar.gz
          tar xzf v1.3.1.tar.gz
          cd zlib-1.3.1
          ./configure --prefix=${BUILD_PREFIX}/libs/zlib
          make -j16
          make install

      - name: Build libpng
        continue-on-error: true
        run: |
          export PATH=${BUILD_PREFIX}/target/bin:$PATH
          export CC=x86_64-apple-darwin17-clang
          export CFLAGS="-mmacosx-version-min=10.13"
          cd ${BUILD_PREFIX}/sources
          git clone --depth=1 https://github.com/glennrp/libpng.git 2>/dev/null || true
          cd libpng
          autoreconf -fiv 2>/dev/null || true
          ./configure --host=x86_64-apple-darwin17 --prefix=${BUILD_PREFIX}/libs/libpng
          make -j16
          make install

      - name: Build libjpeg
        continue-on-error: true
        run: |
          export PATH=${BUILD_PREFIX}/target/bin:$PATH
          cd ${BUILD_PREFIX}/sources
          git clone --depth=1 https://github.com/libjpeg-turbo/libjpeg-turbo.git 2>/dev/null || true
          cd libjpeg-turbo
          mkdir -p build && cd build
          cmake -DCMAKE_SYSTEM_NAME=Darwin -DCMAKE_C_COMPILER=x86_64-apple-darwin17-clang -DCMAKE_CXX_COMPILER=x86_64-apple-darwin17-clang++ -DCMAKE_INSTALL_PREFIX=${BUILD_PREFIX}/libs/libjpeg -DENABLE_SHARED=ON ..
          make -j16
          make install

      - name: Build libtiff
        continue-on-error: true
        run: |
          export PATH=${BUILD_PREFIX}/target/bin:$PATH
          export CC=x86_64-apple-darwin17-clang
          export CFLAGS="-mmacosx-version-min=10.13"
          cd ${BUILD_PREFIX}/sources
          git clone --depth=1 https://github.com/LibTIFF/libtiff.git 2>/dev/null || true
          cd libtiff
          autoreconf -fiv 2>/dev/null || true
          ./configure --host=x86_64-apple-darwin17 --prefix=${BUILD_PREFIX}/libs/libtiff
          make -j16
          make install

      - name: Build libxml2
        continue-on-error: true
        run: |
          export PATH=${BUILD_PREFIX}/target/bin:$PATH
          export CC=x86_64-apple-darwin17-clang
          export CFLAGS="-mmacosx-version-min=10.13"
          cd ${BUILD_PREFIX}/sources
          wget -q https://download.gnome.org/sources/libxml2/2.11/libxml2-2.11.5.tar.xz
          tar xf libxml2-2.11.5.tar.xz
          cd libxml2-2.11.5
          ./configure --host=x86_64-apple-darwin17 --prefix=${BUILD_PREFIX}/libs/libxml2 --without-python
          make -j16
          make install

      - name: Build Boost
        continue-on-error: true
        run: |
          export PATH=${BUILD_PREFIX}/target/bin:$PATH
          cd ${BUILD_PREFIX}/sources
          wget -q https://boostorg.jfrog.io/artifactory/main/release/1.82.0/source/boost_1_82_0.tar.bz2
          tar xjf boost_1_82_0.tar.bz2
          cd boost_1_82_0
          ./bootstrap.sh --prefix=${BUILD_PREFIX}/libs/boost
          ./b2 -j16 --with-system --with-filesystem --with-thread --with-date_time --with-regex --with-chrono toolset=clang cxxflags="-fPIC -stdlib=libc++" link=shared
          ./b2 install

      - name: Create release archive
        run: |
          cd ${BUILD_PREFIX}
          TIMESTAMP=$(date +%Y%m%d_%H%M%S)
          RELEASE_NAME="osxcross-complete-highsierra-${TIMESTAMP}"
          tar --exclude='tarballs' --exclude='sources' --exclude='src/.git' -czf "${RELEASE_NAME}.tar.gz" target/ SDK/ libs/ src/tools 2>/dev/null || tar --exclude='tarballs' --exclude='sources' --exclude='src/.git' -czf "${RELEASE_NAME}.tar.gz" target/ SDK/ libs/
          sha256sum "${RELEASE_NAME}.tar.gz" > "${RELEASE_NAME}.sha256"
          echo "RELEASE_NAME=${RELEASE_NAME}" >> $GITHUB_ENV
          ls -lh osxcross-complete-highsierra-*

      - name: Upload to Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: osxcross-highsierra-complete
          path: |
            ${{ env.BUILD_PREFIX }}/osxcross-complete-highsierra-*.tar.gz
            ${{ env.BUILD_PREFIX }}/osxcross-complete-highsierra-*.sha256
          retention-days: 90

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            ${{ env.BUILD_PREFIX }}/osxcross-complete-highsierra-*.tar.gz
            ${{ env.BUILD_PREFIX }}/osxcross-complete-highsierra-*.sha256
          tag_name: osxcross-highsierra-${{ github.run_number }}
          body: |
            # OSXCross Complete Release - macOS High Sierra (10.13)
            
            **Build Date:** ${{ github.event.head_commit.timestamp || 'Now' }}
            **SDK Version:** 10.13 (High Sierra)
            **Toolchain Target:** x86_64-apple-darwin17
            
            ## Included Components
            - osxcross toolchain (clang, clang++, ar, ranlib, strip, etc.)
            - macOS 10.13 SDK (High Sierra)
            - OpenSSL 1.1
            - zlib 1.3.1
            - libpng
            - libjpeg-turbo
            - libtiff
            - libxml2
            - Boost 1.82
            
            ## Quick Start
            ```bash
            tar xzf osxcross-complete-highsierra-*.tar.gz -C /opt/
            export OSXCROSS_ROOT=/opt/osxcross
            export PATH=$OSXCROSS_ROOT/target/bin:$PATH
            x86_64-apple-darwin17-clang --version
            ```
            
            ## Usage Example
            ```bash
            x86_64-apple-darwin17-clang -mmacosx-version-min=10.13 \
              -isysroot $OSXCROSS_ROOT/SDK/MacOSX10.13.sdk \
              -o program program.c
            ```
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
